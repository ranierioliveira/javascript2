// const numeros = [1, 2, 3, 4, 5, 6];

// const dobros = numeros.map((item) => {
//     return item*2;
// } );

// console.log(dobros);

const palavras = ['abacaxi', 'limão', 'tomate', 'caju'];

const maiusculas = palavras.map(palavra => {
    return palavra.toUpperCase();
});

console.log(maiusculas);